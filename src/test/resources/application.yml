#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb #이러면 메모리 DB를 바탕으로 테스트 실시 데이터베이스 꺼도 테스트가 실시된다.
#                             그러나 놀랍게도 아무런 설정이 없으면 스프링 부트가 기본적으로 메모리 모드로 돌려버린다. 대박!!!
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver


#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
  #       show_sql: true
#        format_sql: true
# 고로 사실 코드 다 필요 없다 ㅎㅎ 

logging:
    level:
      org.hibernate.SQL: debug
